---
import SidebarLayout from '../../layouts/SidebarLayout.astro';
import getPostData from '../../utils/getPostData';

import MetrodroneDemo from '../../components/Metrodrone.svelte';  

export async function getStaticPaths() {
  const posts = await Astro.glob('../../data/projects/*.md');
  return posts.map(p => ({
    params: { slug: p.file.split('/').pop().split('.').shift() },
    props: { post: p },
  }));
}

const { Content, frontmatter } = Astro.props.post;
const { title, description, publishDate, image, tags } = frontmatter;
const { slug, readingTime } = getPostData(Astro.props.post);
const permalink = `${Astro.site.href}projects/${slug}`;

---

<SidebarLayout title={title} description={description} permalink={permalink}>
  <div class="container">
    <a href="/projects" class="back-button">← Back to Projects</a>

    <header>
      <p>{publishDate} ~ {readingTime}</p>
      <h1>{title}</h1>
      <hr />
    </header>
    
    <article class="content">
      <!-- Actual content -->
      <Content />

      <!-- Extras for specific projects -->

    </article>

  </div>
</SidebarLayout>

<style>
  .container {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
  }

  .back-button {
    display: inline-flex;
    align-items: center;
    color: var(--text-secondary);
    text-decoration: none;
    font-size: 0.9rem;
    margin-bottom: 2rem;
    padding: 0.5rem 0;
    transition: color 0.3s ease;
  }

  .back-button:hover {
    color: var(--primary-color);
  }

  header {
    text-align: center;
    margin-bottom: 2rem;
  }

  header h1 {
    margin-bottom: 0.7em;
    color: var(--primary-color);
  }

  header p {
    color: var(--text-secondary);
    text-transform: uppercase;
    font-family: var(--font-family-sans);
    font-weight: 600;
  }

  header hr {
    min-width: 100px;
    width: 30%;
    border: none;
    border-top: 2px solid var(--primary-lighter);
  }

  .content {
    line-height: 1.7;
  }
  

  @media (max-width: 768px) {
    .container {
      padding: 4rem 1rem 1rem;
    }
  }
</style>
